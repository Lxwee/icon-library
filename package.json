{
  "name": "icon-library",
  "version": "1.0.0",
  "description": "Auto-sync icon library",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "build:loader": "node scripts/build-loader.js",
    "build:all": "node scripts/build.js && node scripts/build-loader.js"
  }
}
